<%= form_with(model: salary_type, local: true) do |form| %>
  <% if salary_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(salary_type.errors.count, "error") %> prohibited this salary_type from being saved:</h2>

      <ul>
      <% salary_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :hourly_rate %>
    <%= form.text_field :hourly_rate %>
  </div>

  <div class="field">
    <%= form.label :daily_rate %>
    <%= form.text_field :daily_rate %>
  </div>

  <div class="field">
    <%= form.label :overtime_rate %>
    <%= form.text_field :overtime_rate %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
