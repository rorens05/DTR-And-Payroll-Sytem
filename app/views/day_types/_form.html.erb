<div class="jumbotron form-center">
<%= form_with(model: day_type, local: true) do |form| %>
  <% if day_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(day_type.errors.count, "error") %> prohibited this day_type from being saved:</h2>

      <ul>
      <% day_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :regular %>
    <%= form.check_box :regular %>
  </div>

  <div class="field">
    <%= form.label :date_created %>
    <%= form.date_select :date_created %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
